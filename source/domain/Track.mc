class Track{

	var name;
	var profile;
	var drawPoints;
	var maxPoint;
	
	function initialize(name_,profile_,drawPoints_,maxPoint_) {
        name = name_;
        profile = profile_;
        drawPoints = drawPoints_;
        maxPoint = maxPoint_;
    }
}